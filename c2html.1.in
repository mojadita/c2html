'\" t
.\" ** The above line should force tbl to be a preprocessor **
.\" Man page for c2html
.\"
.\" Copyright (C), @YEAR@ @AUTHOR_NAME@ <@AUTHOR_EMAIL@>
.\"
.\" You may distribute under the terms of the GNU General Public
.\" License as specified in the file COPYING that comes with the
.\" c2html distribution.
.\"
.\" Sat Oct 19 17:01:47 EEST 2024
.\" 
.Dd @BUILD_DATE@
.Dt @PROGNAME@ 1 URM
.Os 
.Sh NAME
.Nm @PROGNAME@
.Nd A program to get HTML, cross referenced pages from a tags file \
and C source code.
.Sh SYNOPSIS
.\" The general command line
.Nm
.Op Fl hnpT
.Op Fl b Ar basedir
.Op Fl d Op Ar 12acDdfIilMmnsx
.Op Fl j Ar javascript_file_name
.Op Fl m Ar default_menu_name
.Op Fl o Ar output
.Op Fl s Ar style_file_name
.Op Fl t Ar tag_file_name
.Sh DESCRIPTION
.Nm
is an utility to generate cross referenced HTML pages from a collection of
C source files and the
.Cm tags
file generated by
.Xr ctags 1
command.
.br
.Nm
constructs a database from the contents of the tags files
referenced in the command line, and then goes along, scanning all the source
files referenced in the database, and HTML formatting the sources,
interspreading
.Cm "<a href>
and
.Cm "<a name>
tags.
It also constructs an
.Cm "index.html
file, or several, related files, depending on command options.
.Pp
It uses 
.B @EX_PATH@
(see
.Xr ex 1 )
to resolve the
.Cm tags
locations, and to edit and insert the 
.Cm "<a name...>
tags.
.Sh OPTIONS
Several options can modify the behaviour of
.Nm
.Bl -tag
.It Fl b
This option allows to specify a
.Ar base_dir
to be used in the html output, so all files are made relative to this base
directory.  By default it generates all paths relative to the file in which
the tag is found.
.It Fl d
This option activates verbose debug mode.
It uses a set of flags as subarguments to indicate the debug output to be
printed to
.Cm stdout .
The flags are:
.Bl -tag
.It Cm 1
Output verbose info about what is happening on the first pass to the
.Cm @DEFAULT_TAG_FILE@
file.
.It Cm 2
Output verbose info (unimplemented yet) about what is happening in the second
pass to the
.Ar tags
database.
In this second pass an index file is constructed to allow to search definitions
without knowing the file they are defined in.
.It Cm a
Allows to activate all the debug output specifying this flag.
This produces large amounts of output, although.
.It Cm c
.Cm tags
processing output.
.It Cm D
Database construction output.
.It Cm d
Directory processing output.
.It Cm f
File processing output.
.It Cm I
String internation output.
.It Cm i
Identifier output.
.It Cm l
Lexical process output.
.It Cm M
Menu creation output.
.It Cm m
Menu processing output.
.It Cm n
Nodes construction output.
.It Cm s
File scanning output.
.It Cm x
commands issued to
.Xr vim 1
(in
.Ar ex
mode)
.El
.It Fl h
.Nm 
outputs a help screen, showing the command line syntax and options help.
.It Fl j
Allows to specify a
.Ar javascript_file_name
(not implemented yet) Defaults to
.Cm @DEFAULT_JS_FILE@.
.It Fl m
Permits to specify a
.Ar menu_name
for the index files.
It defaults to
.Cm @DEFAULT_MENU_BASE@ .
.It Fl p
Shows a progress line on
.Cm stdout ,
showing the file being processed at any time.  It is active by default.
.It Fl s
Allows to specify a
.Ar style_file_name .
By default it uses
.Cm @DEFAULT_STYLE_FILE@ .
.It Fl T
Avoids erasing the
.Ar .c
files that are generated as temporary files.
By default these files are erased.
These files have tags encoded as
.Bd -literal
(@a name="tag-name" @)(@/a@)
.Ed
.Pp
in front of lines that define a symbol.
Later, these pseudotags are converted into:
.Bd -literal
<a name="tag-name" ></a>
.Ed
.Pp
These files are not needed anymore, once the source file has been scanned.
.It Fl t
Allows to specify a
.Ar tags_file_name ,
for the case it is different than the default. (\c
.Cm @DEFAULT_TAG_FILE@ )
.El
.Sh CONFIGURATION PARAMETERS
.Nm
allows configuration parameters to be defined in file
.Ar configure.mk .
These parameters (and their default configured values) are:
.TS
box,tab(");
rb|cb
rf(CW)|l
.
Parameter name"Default value
_
.so configure.1
.TE
.Sh AUTHOR
.An "@AUTHOR_NAME@" Aq Mt @AUTHOR_EMAIL@ .
.Sh COPYRIGHT
Copyright (C) 1999\(em@YEAR@, @AUTHOR_NAME@.  All rights reserved.
.Sh LICENSE
BSD v2.
